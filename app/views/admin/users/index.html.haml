%h1 User's index page

%table
  %thead
    %tr
      %th
        Name
      %th
        E-mail
      %th
        Role
  %tbody
    - @users.each do |user|
      %tr
        %td= user.name
        %td= user.email
        %td= user.role